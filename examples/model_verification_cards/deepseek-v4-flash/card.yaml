# Agent-readable model verification card.
# status: unverified | verified | unsupported | not_applicable

title: deepseek_v4_flash
summary: >
  Performance scope: pretrain_performance.GB200 and pretrain_performance.GB300
  use their tuned canonical 128-GPU MXFP8 recipes. Timing and throughput metrics
  from functional training items are sanity checks rather than optimized
  performance results.

  The Megatron-backed DeepSeek V4 workflows in this card require the
  Megatron-Core dev branch; the default MCore submodule revision in Megatron
  Bridge r0.6.0 is insufficient. Follow the public setup instructions in
  examples/models/deepseek_v4/README.md.

  DeepSeek-V4-Flash verification covers GPU import at the item-pinned Bridge revision.
  The verified 64-GPU GB200 MXFP8 library pretraining run uses PP4/VPP4/EP16 with
  imported model weights, natural routing, grouped GEMM, TE fused cross entropy, and
  selective recompute without activation offload. It completed 100 finite steps with
  full-state checkpoints at steps 50 and 100. A direct step-50 continuation restored
  optimizer, scheduler, data-order, and RNG state, completed steps 51-100, and passed
  both loss-sentinel comparisons. A capacity-1.5 performance screen averaged 206.56
  TFLOP/s/GPU over steps 2-8, but dropped 66.16% of expert assignments and 64.23% of
  routing probability mass; that result is benchmark-only and is excluded from the
  natural-routing library recipe and verification metrics. CPU conversion remains
  unverified because the available nodes lacked sufficient RAM. Standard Megatron
  KV-cache inference is unsupported for DSV4 hybrid attention; inference uses the
  HF-native path after export.

  The GB200 packed SFT recipe uses grouped GEMM, static packed-sequence shapes,
  selective whole-MoE recompute, and attention activation offload while preserving
  the base SFT objective. Its final objective-preserving configuration completed
  100 finite steps and a fresh-process checkpoint reload. GPU export of both the
  imported model and the step-100 SFT checkpoint produced exact BF16 inventories
  and passed independent strict GPU-only HF reloads. The public post-SFT workflow
  also produced identical bounded greedy completions from two independent reloads.
  A separate 100-step CP2 run verifies offline packing with contiguous context
  parallelism.

  The GB200 packed PEFT recipe applies rank-32 LoRA to the DeepSeek MLA projections
  and to separate routed/shared expert FC1/FC2 projections. Its execution path uses
  scoped Transformer Engine CUDA graphs and tuned HybridEP dispatch. It completed
  100 finite steps, saved an adapter-only checkpoint, and reloaded that checkpoint
  in a fresh process for finite step 101.

verification_index:
  model_level:
    verified:
      - hf_to_megatron_gpu
      - megatron_to_hf_gpu
    unverified:
      - hf_to_megatron_cpu
      - inference
      - manual_forward_pass
      - megatron_to_hf_cpu
  training:
    GB200:
      verified: [pretrain, sft, sft_long_context, sft_export_inference, peft, checkpoint_resume]
  performance:
    GB200: verified
    GB300: verified

model:
  hf_id: deepseek-ai/DeepSeek-V4-Flash
  hf_revision: "60d8d70770c6776ff598c94bb586a859a38244f1"  # pragma: allowlist secret
  architecture: DeepseekV4ForCausalLM
  min_transformers_version: "5.12.0"

verification_environment:
  base_container: nvcr.io/nvidia/nemo:26.08
  bridge_commit: 07c5cc30d0510d437bfbbe7b12d9d98387295d89  # pragma: allowlist secret

items:
  hf_to_megatron_cpu:
    status: unverified
    precision: bf16
    command: >
      ./scripts/conversion/convert.sh import --executor slurm --device cpu
      --nodes 1
      --hf-model deepseek-ai/DeepSeek-V4-Flash
      --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
      --megatron-path work/model-verification/dsv4-flash/import-cpu
      --torch-dtype bfloat16 --trust-remote-code
    last_verified: null
    expected_result: >
      CPU import requires approximately 570 GB of CPU RAM for the full BF16 weight
      materialisation (285B parameters x 2 bytes). Verification requires a high-memory
      node with enough additional headroom for conversion workspace and strict checkpoint reload.

  hf_to_megatron_gpu:
    status: verified
    precision: bf16
    bridge_commit: 3ecb452af619bef88083e10a7738362df8f5d0bf  # pragma: allowlist secret
    command: >
      ./scripts/conversion/convert.sh import --executor slurm --device gpu
      --nodes 1 --gpus-per-node 4 --ep 4
      --hf-model deepseek-ai/DeepSeek-V4-Flash
      --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
      --megatron-path work/model-verification/dsv4-flash/import-gpu
      --torch-dtype bfloat16 --trust-remote-code
    last_verified: 2026-08-04
    expected_result: >
      The command exits successfully and creates a reloadable iter_0000000
      Megatron checkpoint.

  megatron_to_hf_cpu:
    status: unverified
    precision: bf16
    command: >
      ./scripts/conversion/convert.sh export --executor slurm --device cpu
      --nodes 1
      --hf-model deepseek-ai/DeepSeek-V4-Flash
      --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
      --megatron-path work/model-verification/dsv4-flash/import-cpu/iter_0000000
      --hf-path work/model-verification/dsv4-flash/export-cpu
      --torch-dtype bfloat16 --trust-remote-code
    last_verified: null
    expected_result: >
      CPU export requires a high-memory node with enough headroom for the full BF16
      weight set and tensor-merging workspace, followed by strict Hugging Face reload.

  megatron_to_hf_gpu:
    status: verified
    precision: bf16
    bridge_commit: 2859a3ee0f02743c54b390de91fee878c6de8909  # pragma: allowlist secret
    command: >
      ./scripts/conversion/convert.sh export --executor slurm --device gpu
      --nodes 2 --gpus-per-node 4 --tp 1 --pp 1 --ep 8 --etp 1
      --hf-model deepseek-ai/DeepSeek-V4-Flash
      --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
      --megatron-path work/model-verification/dsv4-flash/import-gpu/iter_0000000
      --hf-path work/model-verification/dsv4-flash/export-gpu
      --torch-dtype bfloat16 --export-weight-dtype bfloat16 --trust-remote-code
      --not-strict
    last_verified: 2026-08-21
    expected_result: >
      Two GB200 nodes produced 46 complete safetensors shards containing all 35,020
      expected non-scale source keys. BF16 export intentionally omitted 34,167
      quantization-scale companions; 35,017 exported tensors are BF16 and the three
      I32 tid2eid routing tables exactly match their source values after the expected
      integer cast. Two independent Transformers processes each strictly reloaded all
      1,500 model modules across four GPUs with no CPU or disk placement and produced
      the same bounded greedy token. --not-strict permits only the intentional scale
      omission; exact key, shard, dtype, routing-table value, and reload checks remain
      required correctness gates.

  manual_forward_pass:
    status: unverified
    precision: bf16
    command: >
      ./scripts/inference/infer.sh --task model-comparison
      --nodes 1 --gpus-per-node 4 --ep 4
      --hf_model_path deepseek-ai/DeepSeek-V4-Flash
      --megatron_model_path work/model-verification/dsv4-flash/import-gpu/iter_0000000
      --pp 1 --ep 4
      --prompt "The capital of France is the city of"
      --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
    last_verified: null
    expected_result: >
      The attempted four-GPU side-by-side load exceeded the available 768 GB of
      aggregate GPU memory. Verification requires a larger allocation sized from
      measured peak memory; no minimum working topology has been established.

  inference:
    status: unverified
    precision: bf16
    command: >
      ./scripts/inference/infer.sh --nodes 2 --gpus-per-node 4
      --task legacy-full-prefix-generation --legacy-full-prefix
      --hf_model_path deepseek-ai/DeepSeek-V4-Flash
      --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
      --megatron_model_path work/model-verification/dsv4-flash/import-gpu/iter_0000000
      --prompt "The capital of France is" --max_new_tokens 32
      --tp 1 --pp 1 --ep 8 --etp 1 --trust-remote-code
    last_verified: null
    expected_result: >
      Optimized KV-cache autoregressive inference conflicts with
      DSv4HybridAttention's requirement that inference_context be None. The
      maintained legacy full-prefix task disables that context and remains a
      plausible non-cached compatibility path, but the exact model command must
      complete deterministic greedy generation before this item is promoted.

  pretrain:
    GB200:
      status: verified
      precision: fp8_mx
      bridge_commit: dff6445009ebd054028a883ea6d84f9a16ad6b9c  # pragma: allowlist secret
      enabled_features:
        moe_dispatcher: hybridep
      command: >
        ./scripts/training/train.sh --nodes 16 --gpus-per-node 4
        --recipe deepseek_v4_flash_pretrain_64gpu_gb200_fp8mx_library_config
        --mode pretrain --dataset megatron-indexed --max_steps 100
        --pretrained_checkpoint work/model-verification/dsv4-flash/import-gpu/iter_0000000
        'dataset.blend=[["work/data/the-pile/my-gpt3_08_text_document"],null]'
        dataset.path_to_cache=work/cache/the-pile dataset.num_workers=0
        dataset.random_seed=1234 rng.seed=1234
        scheduler.lr_warmup_iters=10 scheduler.lr_decay_iters=100
        validation.eval_interval=0 validation.eval_iters=0
        ddp.check_for_nan_in_grad=true ddp.check_for_large_grads=true
        rerun_state_machine.check_for_nan_in_loss=true
        checkpoint.exit_on_missing_checkpoint=false checkpoint.finetune=false
        checkpoint.load=null checkpoint.load_optim=false checkpoint.load_rng=false
        checkpoint.save_optim=true checkpoint.save_rng=true checkpoint.async_save=false
        --save_dir work/model-verification/dsv4-flash/pretrain-ref
        --save_interval 50
        logger.log_interval=1 logger.log_throughput=true logger.tensorboard_dir=null
        dist.distributed_timeout_minutes=120
      last_verified: 2026-08-27
      metrics:
        initial_loss: 7.250204
        final_loss: 3.280722
        last_10_steps_step_time_ms_avg: 7856.370
        last_10_steps_model_tflops_per_gpu_avg: 189.690
        last_10_steps_tokens_per_second_per_gpu_avg: 2085.441495
      expected_result: >
        On 64 GB200 (16 nodes x 4 GPUs), TP1/PP4/VPP4/EP16/CP1 uses dense DP16,
        expert DP1, and GBS/MBS 256/1. The command loads the imported model weights
        while starting optimizer and RNG state fresh. It uses HybridEP natural routing,
        grouped GEMM, TE fused cross entropy, selective recompute over mhc and
        mla_up_proj, and MXFP8 parameter gather and gradient-buffer reuse without
        activation offload. Expert capacity, paged stash, CUDA graphs, and forced
        load balancing remain disabled. The run completed 100 finite LM/MTP-loss
        steps with no skipped or NaN iterations and wrote complete grouped-MXFP8
        checkpoints containing model, optimizer, scheduler, data-order, and RNG
        state at steps 50 and 100. The item-specific Bridge revision pins the
        compatible Megatron-LM dev revision used by this workload without changing
        the card's default environment for other verification items.

  sft:
    GB200:
      status: verified
      precision: bf16
      enabled_features:
        sequence_packing: offline
        moe_dispatcher: hybridep
      command: >
        ./scripts/training/train.sh --nodes 8 --gpus-per-node 4
        --recipe deepseek_v4_flash_sft_openmath_thinking_packed_gb200_config
        --mode sft --step-func dsv4_step
        --pretrained_checkpoint work/models/deepseek-v4-flash-megatron
        --save_dir work/model-verification/dsv4-flash/sft-ref
        --save_interval 100 --max_steps 100
        --seq_length 1024
        --pipeline_model_parallel_size 4 --context_parallel_size 1
        --expert_model_parallel_size 8
        'model.pipeline_model_parallel_layout=Et*11|t*11|t*11|t*10mL'
        scheduler.lr_warmup_iters=10 scheduler.lr_decay_iters=100
        validation.eval_interval=0 validation.eval_iters=0
        rng.seed=5678
        ddp.check_for_nan_in_grad=true ddp.check_for_large_grads=true
        rerun_state_machine.check_for_nan_in_loss=true
        checkpoint.exit_on_missing_checkpoint=false checkpoint.load=null
        checkpoint.load_optim=false checkpoint.load_rng=false
        checkpoint.save_optim=false
        ddp.overlap_grad_reduce=false
        logger.log_interval=1 logger.log_throughput=true logger.tensorboard_dir=null
        dist.distributed_timeout_minutes=180
      last_verified: 2026-08-20
      metrics:
        initial_loss: 0.9435847
        final_loss: 0.2834835
        last_10_steps_step_time_ms_avg: 8575.85
        last_10_steps_model_tflops_per_gpu_avg: 42.18
        last_10_steps_tokens_per_second_per_gpu_avg: 477.620294
        peak_allocated_memory_gib: 184.96
        peak_reserved_memory_gib: 187.04
      expected_result: >
        On 32 GB200 (8 nodes x 4 GPUs), TP1/PP4/EP8/CP1 with dense DP8 and expert
        DP1, GBS/MBS 128/1. OpenMathInstruct-2 thinking data uses offline packing
        at seq_length=1024 with fixed token and cumulative-boundary shapes. The
        GB200 recipe uses selective recompute over moe, mhc, mla_up_proj, and
        layernorm, attention activation offload, HybridEP dispatch, and DSA kernel
        fusion without enabling the optional DSA indexer loss. The run completed
        100 steps with finite LM and MTP losses, no skipped or NaN iterations, and a
        complete model checkpoint at step 100. A fresh process reloaded that checkpoint
        and completed finite step 101. moe_grouped_gemm=True is a recipe default.
        dist.distributed_timeout_minutes must be 180 or above to allow offline data
        packing (approximately 63 minutes) before training begins.

  sft_export_inference:
    GB200:
      status: verified
      precision: bf16
      bridge_commit: 2859a3ee0f02743c54b390de91fee878c6de8909  # pragma: allowlist secret
      depends_on: sft
      commands:
        - >
          ./scripts/conversion/convert.sh export --executor slurm --device gpu
          --nodes 2 --gpus-per-node 4 --ep 8
          --hf-model deepseek-ai/DeepSeek-V4-Flash
          --hf-revision 60d8d70770c6776ff598c94bb586a859a38244f1
          --megatron-path work/model-verification/dsv4-flash/sft-ref/iter_0000100
          --hf-path work/model-verification/dsv4-flash/sft-export
          --torch-dtype bfloat16 --export-weight-dtype bfloat16 --trust-remote-code
          --not-strict
        - >
          ./scripts/inference/infer.sh --task hf-inference
          --nodes 2 --gpus-per-node 4 --tasks-per-node 1
          --hf-model work/model-verification/dsv4-flash/sft-export
          --prompt "In one short sentence, explain why Paris is important to France."
          --max-new-tokens 32 --chat-template --disable-thinking --trust-remote-code
          --dtype bfloat16 --device-map auto --autocast --require-gpu-only
      last_verified: 2026-08-21
      expected_result: >
        The objective-preserving step-100 SFT checkpoint exported successfully on two
        GB200 nodes. Its 46 complete safetensors shards contain all 35,020 expected
        non-scale source keys: 35,017 BF16 tensors and three value-matched I32 tid2eid
        routing tables, with 34,167 quantization-scale companions intentionally omitted
        for BF16. Two maintained-verifier processes independently reloaded all 1,500 model
        modules across four GPUs each, reported no missing, unexpected, mismatched, or
        error loading entries, used no CPU or disk placement, and generated exactly 32
        new tokens at the 32-token maximum with identical output. The literal completion was: "Paris is
        important to France because it is the country's capital, a major economic and
        cultural center, and a symbol of French history, art, and architecture."

  sft_long_context:
    GB200:
      status: verified
      precision: bf16
      bridge_commit: 3ecb452af619bef88083e10a7738362df8f5d0bf  # pragma: allowlist secret
      enabled_features:
        sequence_packing: offline
        context_parallel_size: 2
      command: >
        ./scripts/training/train.sh --nodes 16 --gpus-per-node 4
        --recipe deepseek_v4_flash_sft_openmath_thinking_packed_config
        --step-func dsv4_step
        --pretrained_checkpoint work/models/deepseek-v4-flash-megatron
        --save_dir work/model-verification/dsv4-flash/sft-long-context-ref
        --save_interval 100 --max_steps 100
        model.pipeline_model_parallel_size=4
        'model.pipeline_model_parallel_layout=Et*11|t*11|t*11|t*10mL'
        model.context_parallel_size=2 model.cp_partition_mode=contiguous
        model.expert_model_parallel_size=8
        model.moe_grouped_gemm=false
        model.recompute_granularity=full model.recompute_method=uniform
        model.recompute_num_layers=1
        dataset.seq_length=1024
        scheduler.lr_warmup_iters=10 scheduler.lr_decay_iters=100
        validation.eval_interval=0 validation.eval_iters=0
        rng.seed=5678
        ddp.check_for_nan_in_grad=true ddp.check_for_large_grads=true
        rerun_state_machine.check_for_nan_in_loss=true
        checkpoint.exit_on_missing_checkpoint=false checkpoint.load=null
        ddp.overlap_grad_reduce=false
        logger.log_interval=1 logger.log_throughput=true logger.tensorboard_dir=null
        dist.distributed_timeout_minutes=180
      last_verified: 2026-08-07
      metrics:
        initial_loss: 0.94383
        final_loss: 0.28370
        last_10_steps_step_time_ms_avg: 13806.500
        last_10_steps_model_tflops_per_gpu_avg: 13.620
        last_10_steps_tokens_per_second_per_gpu_avg: 148.336
      expected_result: >
        CP=2 with contiguous partitioning and offline-packed OpenMathInstruct-2
        at seq_length=1024 demonstrates sequence packing and context parallelism
        working together over 100 training steps with finite loss and no skipped
        or NaN iterations.

  peft:
    GB200:
      status: verified
      precision: bf16
      bridge_commit: 043d9cdaa1ac0cb52437d8d417a9f3150dfcae3b  # pragma: allowlist secret
      command: >
        ./scripts/training/train.sh --nodes 8 --gpus-per-node 4
        --recipe deepseek_v4_flash_peft_openmath_thinking_packed_gb200_config
        --mode lora --step-func dsv4_step
        --pretrained_checkpoint work/models/deepseek-v4-flash-megatron
        --save_dir work/model-verification/dsv4-flash/peft-ref
        --save_interval 100 --max_steps 100
        --seq_length 1024
        --pipeline_model_parallel_size 4 --context_parallel_size 1
        --expert_model_parallel_size 8
        'model.pipeline_model_parallel_layout=Et*11|t*11|t*11|t*10mL'
        scheduler.lr_warmup_iters=10 scheduler.lr_decay_iters=100
        validation.eval_interval=0 validation.eval_iters=0
        rng.seed=5678
        ddp.check_for_nan_in_grad=true ddp.check_for_large_grads=true
        rerun_state_machine.check_for_nan_in_loss=true
        checkpoint.exit_on_missing_checkpoint=false checkpoint.load=null
        checkpoint.load_optim=false checkpoint.load_rng=false
        checkpoint.save_optim=false
        ddp.overlap_grad_reduce=false
        logger.log_interval=1 logger.log_throughput=true logger.tensorboard_dir=null
        dist.distributed_timeout_minutes=180
      last_verified: 2026-08-25
      enabled_features:
        sequence_packing: offline
        cuda_graph:
          implementation: transformer_engine
          scopes: [moe_router, moe_preprocess]
        moe_dispatcher: hybridep
      metrics:
        initial_loss: 0.9435847
        final_loss: 0.2914867
        last_10_steps_step_time_ms_avg: 6944.33
        last_10_steps_model_tflops_per_gpu_avg: 52.00
        last_10_steps_tokens_per_second_per_gpu_avg: 589.833720
        peak_allocated_memory_gib: 53.712
        peak_reserved_memory_gib: 53.953
      expected_result: >
        On 32 GB200 (8 nodes x 4 GPUs), TP1/PP4/EP8/CP1 with dense DP8 and expert
        DP1, GBS/MBS 128/1. The recipe applies rank-32, alpha-32 LoRA with zero
        dropout to linear_q_down_proj, linear_q_up_proj, linear_kv_proj,
        linear_proj, linear_fc1, and linear_fc2. Routed experts use separate
        per-expert adapters; shared experts are also adapted. The run preserves
        the packed OpenMath thinking data, objective, natural routing, seed, and
        warmup/decay horizon used by the verified SFT item while freezing the base
        model and using PEFT's own 1e-4 peak learning rate.
        It disables activation recompute and offload because the reduced PEFT
        training-state footprint fits with 53.712 GiB peak allocated memory.
        Transformer Engine CUDA graphs cover moe_router and moe_preprocess, while
        HybridEP uses 32 flex-dispatcher SMs, 8 ranks per NVLink domain, 128-token
        combine chunks, a 72-GPU domain, and MNNVL. All 100 steps completed with
        finite LM/MTP losses and zero skipped or NaN iterations. LM/MTP losses
        were 0.9435847/0.2250465 at step 1 and 0.2914867/0.02638751 at step 100.
        The adapter-only checkpoint
        reloaded after the same base checkpoint in a fresh process and completed
        finite step 101 with LM loss 0.2827368 and MTP loss 0.02594211.

  checkpoint_resume:
    GB200:
      status: verified
      precision: fp8_mx
      bridge_commit: dff6445009ebd054028a883ea6d84f9a16ad6b9c  # pragma: allowlist secret
      depends_on: pretrain
      command: >
        ./scripts/training/train.sh --nodes 16 --gpus-per-node 4
        --recipe deepseek_v4_flash_pretrain_64gpu_gb200_fp8mx_library_config
        --mode pretrain --dataset megatron-indexed --max_steps 100
        'dataset.blend=[["work/data/the-pile/my-gpt3_08_text_document"],null]'
        dataset.path_to_cache=work/cache/the-pile dataset.num_workers=0
        dataset.random_seed=1234 rng.seed=1234
        scheduler.lr_warmup_iters=10 scheduler.lr_decay_iters=100
        validation.eval_interval=0 validation.eval_iters=0
        ddp.check_for_nan_in_grad=true ddp.check_for_large_grads=true
        rerun_state_machine.check_for_nan_in_loss=true
        checkpoint.exit_on_missing_checkpoint=false checkpoint.finetune=false
        checkpoint.load_optim=true checkpoint.load_rng=true
        checkpoint.save_optim=true checkpoint.save_rng=true checkpoint.async_save=false
        --load_dir work/model-verification/dsv4-flash/pretrain-ref
        --save_dir work/model-verification/dsv4-flash/pretrain-resumed
        --save_interval 50 checkpoint.ckpt_step=50
        logger.log_interval=1 logger.log_throughput=true logger.tensorboard_dir=null
        dist.distributed_timeout_minutes=120
      last_verified: 2026-08-27
      metrics:
        initial_loss: 4.028752
        final_loss: 3.261145
        last_10_steps_step_time_ms_avg: 7995.850
        last_10_steps_model_tflops_per_gpu_avg: 186.640
        last_10_steps_tokens_per_second_per_gpu_avg: 2049.062951
      resume_comparison:
        reference_item: pretrain
        sentinel_steps: [51, 100]
        loss_relative_tolerance: 1.0e-2
        loss_absolute_tolerance: 1.0e-6
        sentinels_match: true
      expected_result: >
        The direct 64-GPU TP1/PP4/VPP4/EP16 continuation restores model, optimizer,
        scheduler, data-order, and RNG state from the complete step-50 reference
        checkpoint, starts at step 51, and finishes at step 100 in a distinct output
        root. All 50 resumed steps are finite with no skipped or NaN iterations, all
        five metrics are recorded, the post-setup configuration persists, and a
        complete full-state step-100 checkpoint is saved. Step-51 resumed/reference
        LM loss is 4.028752/4.028773, an absolute difference of 0.000021. Step-100
        resumed/reference LM loss is 3.261145/3.280722, an absolute difference of
        0.019577. Both sentinels pass the declared 1% relative and 1e-6 absolute gate.

  pretrain_performance:
    GB200:
      status: verified
      precision: fp8_mx
      bridge_commit: 3ecb452af619bef88083e10a7738362df8f5d0bf  # pragma: allowlist secret
      command: >
        ./scripts/training/train.sh --nodes 32 --gpus-per-node 4
        --recipe deepseek_v4_flash_pretrain_128gpu_gb200_fp8mx_config
        --max_steps 50
        scheduler.lr_warmup_iters=5 scheduler.lr_decay_iters=50
        validation.eval_interval=0 validation.eval_iters=0
        checkpoint.exit_on_missing_checkpoint=false
        logger.log_interval=1 logger.log_throughput=true logger.tensorboard_dir=null
        dist.distributed_timeout_minutes=120
      last_verified: 2026-08-06
      metrics:
        initial_loss: 13.58750
        final_loss: 3.62683
        last_10_steps_step_time_ms_avg: 8134.600
        last_10_steps_model_tflops_per_gpu_avg: 731.400
        last_10_steps_tokens_per_second_per_gpu_avg: 8056.450
      expected_result: >
        On 128 GB200, PP1/EP64/CP1/TP1/DP2 with GBS/MBS 2048/1, FP8-MX,
        HybridEP dispatch, and full-iteration CUDA graphs completes 50 steps
        with finite losses, no skipped or NaN iterations, and all five metrics
        recorded. This benchmark uses forced routing, a static expert-rank
        capacity, paged stash, and a different batch/objective contract, so its
        metrics must not be compared with natural-routing library pretraining
        as convergence evidence.

    GB300:
      status: verified
      precision: fp8_mx
      bridge_commit: c93251151adeeadbae3ff2a2bf5ee7a1c34cff01  # pragma: allowlist secret
      command: >
        ./scripts/training/train.sh --wait --nodes 32 --gpus-per-node 4
        --recipe deepseek_v4_flash_pretrain_128gpu_gb300_fp8mx_config
        --mode pretrain --max_steps 50 --seq_length 4096
        logger.save_config_filepath=work/model-verification/deepseek-v4-flash/gb300-performance/ConfigContainer.yaml
      last_verified: 2026-08-13
      metrics:
        initial_loss: 12.59616
        final_loss: 3.160061
        last_10_steps_step_time_ms_avg: 7840.320
        last_10_steps_model_tflops_per_gpu_avg: 759.280
        last_10_steps_tokens_per_second_per_gpu_avg: 8358.842
      expected_result: >
        On exactly 128 GB300s, the canonical MXFP8 mock-data recipe completes
        exactly 50 optimizer steps at TP1/PP1/CP1/EP64/ETP1, GBS/MBS 2048/1,
        and sequence length 4096. All 50 keyed rows have finite loss with zero
        skipped or NaN iterations. Loss moves from 12.59616 to 3.160061; the
        final ten steps average 7840.320 ms, 759.280 TFLOP/s/GPU, and 8358.842
        tokens/s/GPU. The resolved configuration persists.
